[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "bf"
description = ""
readme = "README.md"
version = "0.1.0"
requires-python = "== 3.10.14"
dependencies = []
